

export const BASE_URL = import.meta.env.BACKEND_URL; // Ensure to set this to your backend server URL
export const PRODUCT_URL = "/api/products"; // Relevant for product-related actions
// Removed unnecessary URLs: USERS_URL, CATEGORY_URL, UPLOAD_URL, ORDERS_URL, PAYPAL_URL
